experiment:
  name: "art-e-multihop"
  seed: 42
  num_samples: 350
  max_turns: 10
  output_dir: "outputs/eval"

dataset:
  path: "data/art_e_vince_kaminski_multihop"
  split: "test"
  db_path: "data/enron_emails.db"

judge:
  model: "gpt-5-mini"
  api_key_env: "OPENAI_API_KEY"

models:
  - name: "gpt-5"
    backend: "openai"
    model_name: "gpt-5"
    api_key_env: "OPENAI_API_KEY"
    temperature: 0.0
    max_tokens: 4096
    max_concurrent: 10

  - name: "gpt-5-mini"
    backend: "openai"
    model_name: "gpt-5-mini"
    api_key_env: "OPENAI_API_KEY"
    temperature: 0.0
    max_tokens: 4096
    max_concurrent: 10
